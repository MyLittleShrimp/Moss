# G11.1 发布版稀有保底调整交接

日期：2026-09-12，版本v0.11.1。

## 范围与改动

按用户要求，发布版东京最大保底次数从3改为5，伊斯坦布尔从3改为4；巴黎和冰岛仍为3，开发版仍为8。计数仍是实际成功到访，折返不计。每次未中增加25个百分点的长途概率规则保留。已出发旅程的掉落快照不重新抽取，已有连续未中次数不清零。

修改scripts/discovery_world.gd、tests/test_settings.gd；同步数值文档、README、STATUS和CHANGELOG。游戏中的保底文案从同一函数读取，无需另外手写数字。

## 验证

artifacts/g11-1-rules.log：325项规则通过，新增东京5次、伊斯坦布尔4次阈值断言、最终次数必得和第三次仍可能未中的反例。测试使用artifacts/内存存档，没有读取玩家Key或操作玩家存档。构建两种便携包通过。本轮没有遇到新的实质问题；受限headless证书提示沿用既有环境说明，未新增排障条目。

## 运行与版本

关闭旧窗口。源码使用StartGame.cmd，发布预览使用StartReleasePreview.cmd。更新便携包为dist/Moss-Release-v0.11.1-Windows.zip和Moss-Development-v0.11.1-Windows.zip，解压整个文件夹运行Start.cmd。旧版本压缩包保留。

本地develop提交后main快进，标签v0.11.1；无远程上传。此为数值小调整，不代表长期平衡已经获得用户数据验证。下一步继续观察两条长途奖励等待体验。
