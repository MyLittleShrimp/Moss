# G2 交接：验证与原型交付

日期：2026-09-09。状态：完成本轮 G2。

## 交付入口

- 直接游玩：项目根目录双击 `StartGame.cmd`。
- 编辑：`./run.ps1 -Editor` 或用 Godot 4.6.2 导入 `project.godot`。
- 安装/资源导入：新机器 `./setup.ps1`。
- 使用说明：`README.md`。
- 问题手册：`docs/TROUBLESHOOTING.md`。

## 实际执行的验证

| 验证 | 实际结果 | 证据 |
|---|---|---|
| Godot 引擎版本 | 4.6.2.stable.official.71f334935 | 引擎 `--version` 输出 |
| 规则测试 | 23/23 PASS，最终日志无 ERROR | `artifacts/world-tests.log` |
| 原生图形渲染 | OpenGL 3.3，NVIDIA RTX 2080 Ti，Compatibility | `artifacts/ui-qa.log` |
| UI 操作闭环 | 使用 Godot 注入鼠标按下/释放事件点击实际按钮，PASS | `scripts/main.gd` 的 `qa_click`、`qa_flow` |
| 布局检查 | 修复贴图尺寸后再次渲染和检查；中文、角色和庭院位置正常 | 01–04 截图 |
| 角色透明背景 | RGBA，alpha 最小 0 最大 255 | 素材检查与实际透明叠加 |
| 存档恢复 | 独立测试存档故意损坏后，从备份恢复并保留坏文件 | 规则测试输出 |

截图为实际 Godot viewport 输出，不是网页复刻或设计稿。

## 截图索引

- `artifacts/01-home.png`：初始小屋、三块庭院、苔苔与操作区。
- `artifacts/02-travelling.png`：旅行中状态、倒计时、角色不在家。
- `artifacts/03-letter.png`：归来、青石奖励、关联雨声偏好的信。
- `artifacts/04-memory.png`：偏好来源、旅行次数与窗台上的青石。

## 验证边界

UI_QA 使用内存状态，推进计时值以缩短测试；不改用户存档。鼠标事件走 Godot 的 UI 路由，尚未做外部真人完整 1 分钟试玩。没有执行 14 天留存观察、移动端兼容性、发布 EXE 验证、网络模型调用或并发服务器测试。

当前窗口按 1440×900 逻辑布局缩放，本机 1280×800 实际截图；其他分辨率仅有最小窗口和等比缩放配置，仍需更广泛验证。中文字体使用 Windows Microsoft YaHei，其他系统依赖其 CJK 字体，尚未随包分发字体。

## 已处理问题

缺少 Godot、Schannel 下载失败、AppData 沙箱访问、TextureRect 尺寸顺序、JSON 预期错误日志已分别记录为 TS-001–005。TS-003 的受限环境边界仍存在，正常桌面运行已通过；没有通过关闭 TLS 校验或扩大项目权限来规避。

## 下一阶段建议（尚未完成）

1. 用户试玩后校准角色大小、场景构图、操作节奏和文本长度。
2. 选择真实 LLM 供应商，建立服务端代理与成本/超时/输出契约；客户端继续提供可玩降级。
3. 加入 6 个结构化旅行事件与一条可验证的记忆反馈链，先验证差异再扩内容。
4. 将宠物拆成可动画层或制作关键姿势，补足移动与家具遮挡。
5. 完成服务端权威状态后接只读 MCP，再扩受限照料。

下一阶段仍需独立 handoff，遇到新问题追加 troubleshooting 条目，不覆盖旧问题的复现与解决记录。
