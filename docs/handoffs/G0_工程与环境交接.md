# G0 交接：Godot 路线与运行环境

日期：2026-09-09。状态：完成本轮 G0 工程准备；不代表原计划 P0 访谈和市场验证完成。

## 已完成

- 技术路线根据用户要求切换为 Godot 4 / GDScript、Compatibility 桌面原型。
- 官方便携引擎已在 `.tools/godot`，版本 `4.6.2.stable.official.71f334935`。
- 建立 `project.godot`、入口场景、`run.ps1`、`setup.ps1`、`.gitignore`。
- 将每阶段 handoff 与问题手册要求写入项目 `AGENTS.md`。
- 两张 ImageGen 原创图已入库；尺寸、透明度和提示词记录在 `docs/ASSET_PROVENANCE.md`。

## 运行

工作目录为项目根目录。执行 `./run.ps1 -Editor` 进入编辑器；`./run.ps1` 运行游戏。换机器先执行 `./setup.ps1`（需要 Python 3 和网络），或用 `-GodotPath` 指向现有 Godot 4.6.2。

## 实际验证与问题

引擎版本已读取，资源成功导入。原生图形运行确认 NVIDIA OpenGL 3.3 Compatibility。下载 Schannel 错误、沙箱 AppData 访问问题见 `docs/TROUBLESHOOTING.md` TS-001–003。

## 下一阶段

G1 交付本地完整玩法循环；G2 完成规则测试、原生渲染截图检查和使用说明。暂未下载独立导出模板，当前以 Godot 工程方式运行，不是独立发布 EXE。
