# G5 宣传截图交接

日期：2026-09-10。范围：四张原生截图与短文案，不改变游戏规则。

## 文件与运行

- `tests/capture_press.gd`、`tests/press_scene.tscn`：继承现有游戏界面的独立展示夹具，必须传 `--qa`；断言空存档路径。
- `artifacts/press/g5-01-home.png` 至 `g5-04-postcards.png`：小屋、种植、跨洲行囊、海外明信片，1920×1200。
- `artifacts/press/发推文案.md`：可复制文案、顺序和素材边界。
- `artifacts/苔间小屋-发推截图.zip`：四张 PNG 与文案。

复现：Godot `--path . res://tests/press_scene.tscn -- --qa`。输出目录需存在。

## 验证

实际运行退出码 0，`artifacts/press/capture.log` 有 PRESS_CAPTURE_PASS。四张图均已打开检查。截图通过 Godot viewport 原生保存，未做后期修图，没有模型调用，没有读取或写入玩家存档。

## 限制与下一步

展示进度为内存夹具；图片仍反映实际可用界面。AI 开关关闭，不将本地对话充作 LLM 证据；明信片为预生成素材。未发推，用户可自行选择配图和文案。本轮无新增实质故障，沿用现有 TROUBLESHOOTING 手册。
